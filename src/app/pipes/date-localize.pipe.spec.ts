import { DateLocalizePipe } from './date-localize.pipe';

describe('DateLocalizePipe', () => {
  it('create an instance', () => {
    const pipe = new DateLocalizePipe();
    expect(pipe).toBeTruthy();
  });
});
